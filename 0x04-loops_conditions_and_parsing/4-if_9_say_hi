#!/usr/bin/env bash
# Displays "Best School" 10 times with "Hi" on the 9th iteration
count=1
while [ $count -le 10 ]; do
    echo "Best School"
    if [ $count -eq 9 ]; then
        echo "Hi"
    fi
    count=$((count + 1))
done
